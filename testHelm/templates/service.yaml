apiVersion: v1
kind: Service
metadata:
  name: {{ include "testHelm.fullname" . }}
  labels:
    {{- include "testHelm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "testHelm.selectorLabels" . | nindent 4 }}
